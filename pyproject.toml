[project]
name = "fastapi-mcp-server"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "load-dotenv>=0.1.0",
    "mcp>=1.9.4",
    "pyodbc>=5.2.0",
    "starlette>=0.47.0",
]
